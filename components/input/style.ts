import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    input: {
        width: '100%',
        height: 50,
        backgroundColor: '#fff',
        borderRadius: 3,
        fontSize: 20,
        fontWeight: 'bold',
        paddingLeft: 20
    }
})

export default styles